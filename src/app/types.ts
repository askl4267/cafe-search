export type MidArea = {
  name: string;
  count: number;
  selected?: boolean;
};

export type SmallArea = {
  name: string;
  count: number;
  checked?: boolean;
};

export type Cafe = {
  name: string;
  description: string;
  address: string;
  parking: string;
  wifi: string;
  nonSmoking: string;
  image: string;
};
